# Student Management System (Express CRUD)

Backend CRUD application using Node.js, Express, and ES Modules.
Student data is stored in db.json with file system persistence.

## Run Project
npm install
npm start

## API Endpoints
GET /students
POST /students
PUT /students/:id
DELETE /students/:id
